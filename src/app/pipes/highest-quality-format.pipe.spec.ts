import { HighestQualityFormatPipe } from './highest-quality-format.pipe';

describe('HighestQualityFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HighestQualityFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
